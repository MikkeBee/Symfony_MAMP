{% extends 'base.html.twig' %}

{% block title %}Hello FruitController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

<form action="{{path('create_fruit')}}" method="post">
<input type="text" name="fruitname" id="" placeholder="Enter your favourite fruit"/>
<input type="text" name="color" id="" placeholder="Enter the color"/>
<input type="text" name="taste" id="" placeholder="How does it taste?"/>
<button type="submit" class="addBtn">Add fruit</button>
</form>

    <h1>🍊🍌🥥 Fruits! 🥝🍓🫐</h1>
    {# <ul>
        {% for fruit in fruits %}
        <li> {{fruit.fruitname}} - {{fruit.color}} - {{fruit.taste}}</li>
        <a class="updateBtn" href="{{path('update_fruit', {'id': fruit.id})}}">Update</a>
        <a class="deleteBtn" href="{{path('delete_fruit', {'id': fruit.id})}}">Delete </a>
        {% endfor %}
    </ul> #}
    {# <form method="post">
    {% for fruit in fruits %}    
    <input type="text" name="fruitname" id="" value={{fruit.fruitname}}/>
<input type="text" name="color" id="" value={{fruit.color}}/>
<input type="text" name="taste" id="" value={{fruit.taste}}/>
<a href="{{path('update_fruit', {'id': fruit.id})}}" class="addBtn">Update fruit</a>
{% endfor %}

</form> #}
{% for fruit in fruits %}
    
    <form method:"PATCH" action="{{path('update_fruit', {'id': fruit.id})}}" >
    <input type="text" name="fruitname" value={{fruit.fruitname}}>
    <input type="text" name="color" value={{fruit.color}}>
    <input type="text" name="taste" value={{fruit.taste}}>
    <button type="submit">afsd</button>
    </form> 
        
        {# <a href="{{path('update',{'id': fruit.id})}}">Update</a> #}
        <a href="{{path('delete_fruit', {'id': fruit.id})}}">Delete</a>
        
    {% endfor %}



</div>
{% endblock %}
